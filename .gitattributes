# Shell scripts and git hooks run on the Linux VM (clawbot-v2) — force LF so a Windows
# checkout/edit can't break the shebang or the git filter-repo / systemd scripts with CRLF.
*.sh        text eol=lf
ops/git-hooks/* text eol=lf
deploy/systemd/* text eol=lf
